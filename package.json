{
  "name": "contentos",
  "version": "0.1.0",
  "private": true,
  "description": "ContentOS — B2B LinkedIn image content operating system",
  "scripts": {
    "dev": "npm run dev:api",
    "dev:api": "cd apps/api && .venv/bin/uvicorn src.dev_server:app --reload --port 4001",
    "dev:web": "cd apps/web && npm run dev",
    "dev:marketing": "cd apps/marketing && npm run dev",
    "install:api": "cd apps/api && python3 -m venv .venv && .venv/bin/pip install -U pip setuptools wheel && .venv/bin/pip install -e '.[dev]'",
    "install:web": "cd apps/web && npm install",
    "install:marketing": "cd apps/marketing && npm install",
    "install:infra": "cd infra && ./scripts/ensure-venv.sh",
    "build:layer": "cd apps/api && .venv/bin/python layers/shared/python/scripts/build_layer.py",
    "build:web": "cd apps/web && npm run build",
    "build:web:deploy": "VITE_BASE=/app/ infra/scripts/build-web.sh",
    "build:marketing": "cd apps/marketing && npm ci && npm run build",
    "db:init": "cd apps/api && .venv/bin/python scripts/seed.py",
    "test": "cd apps/api && .venv/bin/python -m pytest",
    "synth:dev": "npm run build:layer && npm run build:web:deploy && npm run build:marketing && cd infra && ./scripts/deploy.sh dev synth",
    "deploy:dev": "npm run build:layer && npm run build:web:deploy && npm run build:marketing && cd infra && ./scripts/deploy.sh dev deploy && ./scripts/publish-web.sh dev",
    "deploy:qa": "npm run build:layer && npm run build:web:deploy && npm run build:marketing && cd infra && ./scripts/deploy.sh qa deploy && ./scripts/publish-web.sh qa",
    "deploy:prod": "npm run build:layer && npm run build:web:deploy && npm run build:marketing && cd infra && ./scripts/deploy.sh prod deploy && ./scripts/publish-web.sh prod"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
