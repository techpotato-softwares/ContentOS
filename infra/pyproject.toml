[project]
name = "infra"
version = "0.1.0"
description = "Infrastructure setup using AWS CDK"
requires-python = ">=3.10"
dependencies = [
    "aws-cdk-lib>=2.256.1,<3",
    "constructs>=10.3.0,<11",
]

[tool.uv]
package = false
